node_modules
.DS_Store
dist
dist-ssr
*.local
*.local.*
count.txt
.env*
!.env.example
!.env.local.example
!.env.local.screenshots.example
.nitro
.tanstack
.wrangler
.output
.vinxi
.vercel
storybook-static


# Screenshot generator artifacts
/screenshots/
.cache/screenshots/
scripts/screenshots/.fixture-ids.json

# AGENTS
# core/docs is a symlink into the parent repo's agents/core (workspace-owned docs).
# No trailing slash so the symlink (a file) is matched, not just a real directory.
/docs
.agents/*
.claude/*
CLAUDE.local.md
CLAUDE.md

